<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>GitHelp</title>


  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.2/css/font-awesome.min.css">

  <style>
    /* Only necessary if you are using navbar-fixed-top */
    body {
      padding-top: 50px;
    }
  </style>

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.2/js/bootstrap.min.js"></script>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to Rails.application.class.parent_name, root_url, :class => "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Chores", chores_url %></li>
        <li><%= link_to "Task Havers", task_havers_url %></li>
        <li><%= link_to "Students", students_url %></li>
        <li><%= link_to "Type Of Chores", type_of_chores_url %></li>
        <% if student_signed_in? %>
          <li><%= link_to current_student.email, edit_student_url(current_student.id) %></li>
          <li><%= link_to "Sign Out", destroy_student_session_path, :method => 'delete' %></li>

        <% elsif task_haver_signed_in? %>
          <li><%= link_to current_task_haver.email, edit_task_haver_url(current_task_haver.id) %></li>
          <li><%= link_to "Sign Out", destroy_task_haver_session_path, :method => 'delete' %></li>

        <% else %>
          <li><%= link_to "Sign Up As Student", new_student_registration_path %></li>
          <li><%= link_to "Sign In As Student", new_student_session_path %></li>
          <li><%= link_to "Sign Up As TaskHaver", new_task_haver_session_path %></li>
          <li><%= link_to "Sign In As TaskHaver", new_task_haver_session_path %></li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </nav>

  <div class="container">
    <% if notice.present? %>
      <div class="alert alert-dismissable alert-success">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= notice %>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="alert alert-dismissable alert-warning">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
